# Server configuration
PORT=5000

# MongoDB connection (local instance) used by Prisma.
# NOTE: Prisma's MongoDB connector requires the database to run as a replica set.
DATABASE_URL=mongodb://localhost:27017/users_demo

# CORS - allowed origin of the React client
CLIENT_ORIGIN=http://localhost:5173
